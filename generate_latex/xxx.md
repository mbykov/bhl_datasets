неверно подставляет названия переменной: с умножить на кэпа меньше
не понимает действительно непонятную конструкцию \cdot в начале строки
и не понимает \cdot после арифметики: "/ \cdot", как в начале строки
не правильно понимает названия символов латеха - интеграл по двойному - нужен двойной интеграл

в прошлых вариантах таких ошибок не было.

Нужно как было - сначала определить все названия переменных и символов латеха на языке (rus, eng). Пользуясь данными в папке config

получить тупую заглушку только на русском / английском. Тут можно вывести предупреждение, что что-то не перевелось, если не перевелось.

только затем уже обратиться к LLM, чтобы она грамотно оформила текст из заглушки в соответствии со стилем.

В промпте указать:
Не изменяя названия переменных никак, можно только склонять их.
А названиям символов можно присваивать синонимичные значения, если требуется стилем.
Например "скобка А плюс В скобка в степени два", или "А и В все в квадрате"
